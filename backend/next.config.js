// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["ik.imagekit.io"], // 👈 habilita tu dominio de ImageKit
  },
};

module.exports = nextConfig;
